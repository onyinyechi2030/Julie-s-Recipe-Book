# Julie’s Recipe Book v11

Open `index.html` directly. This build contains all 301 recipes in one file.

Search now supports multiple independent words, common synonyms, ingredients, descriptions, cuisine, protein, tags, and instructions. Use **Clear** or **Reset all filters** if a selected filter is hiding otherwise valid results.
